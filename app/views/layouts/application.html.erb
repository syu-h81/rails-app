<!DOCTYPE html>
<html>
  <head>
    <title>TweetApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="header">
      <div class="header-Inner">
        <div class="header-Logo">
          <h1 class="logo">TweetApp</h1>
        </div>
        <nav class="header-Nav">
          <ul class="header-Nav_list">
            <li class="header-Nav_item"><%= link_to("TweetAppとは", "/about") %></li>
            <li class="header-Nav_item"><%= link_to("ログイン", "/") %></li>
            <li class="header-Nav_item"><%= link_to("新規登録", "/") %></li>
          </ul>
        </nav>
      </div>
    </header>
    <%= yield %>
  </body>
</html>
